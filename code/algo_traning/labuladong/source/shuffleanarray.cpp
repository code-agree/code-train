#include <vector>

using namespace std;

template <struct T>
struct Person{
public:
	T age;
};


alist = range(1, 8);
blist = [i + 1 for i in alist if i % 4 == 0]
print(blist)

int main(){
    return 0;
}